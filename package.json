{
  "name": "system-z",
  "version": "0.1.0",
  "description": "displays system info",
  "main": "bin/system-z.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "system-z": "./bin/system-z.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^7.2.0"
  }
}
